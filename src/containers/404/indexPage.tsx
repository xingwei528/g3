import * as React from 'react';
import { Link } from 'react-router';

const NotFoundPage = () => {
  return (
    <h4 style={{ margin: "auto 0", padding: "140px", textAlign: "center", fontSize: "22px"}}>
      页面未找到
    </h4>
  );
};

export default NotFoundPage;
